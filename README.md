# 📷 カメラクイズ - CAMERA QUIZ

カメラと写真の雑学を楽しく学べるWebクイズアプリです。

## 機能

- ✅ `questions.json` からランダム10問を出題
- ✅ 4択形式
- ✅ 正誤判定と解説表示
- ✅ スコア集計と結果画面
- ✅ もう一度挑戦機能

## 起動手順

### 1. 依存パッケージをインストール

```bash
cd camera-quiz
npm install
```

### 2. 開発サーバーを起動

```bash
npm run dev
```

ブラウザで `http://localhost:5173` を開きます。

### 3. 本番ビルド（オプション）

```bash
npm run build
npm run preview
```

## ファイル構成

```
camera-quiz/
├── src/
│   ├── components/
│   │   ├── StartScreen.tsx    # スタート画面
│   │   ├── Question.tsx       # 問題表示・選択肢
│   │   └── Result.tsx         # 結果画面
│   ├── hooks/
│   │   └── useQuiz.ts         # クイズロジック
│   ├── data/
│   │   └── questions.json     # 問題データ
│   ├── types.ts               # 型定義
│   ├── App.tsx                # メインコンポーネント
│   ├── main.tsx               # エントリーポイント
│   └── index.css              # スタイル
├── index.html
├── package.json
├── vite.config.ts
└── tsconfig.json
```

## 問題データの追加方法

`src/data/questions.json` に以下の形式で追加：

```json
{
  "id": 11,
  "category": "カテゴリ名",
  "difficulty": 2,
  "question": "問題文？",
  "choices": ["選択肢A", "選択肢B", "選択肢C", "選択肢D"],
  "answer": 0,
  "explanation": "解説文"
}
```

### フィールド説明

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `id` | number | 一意のID |
| `category` | string | カテゴリ（基礎用語、レンズ、フィルムなど） |
| `difficulty` | 1-3 | 難易度（★☆☆ / ★★☆ / ★★★） |
| `question` | string | 問題文 |
| `choices` | string[] | 4つの選択肢 |
| `answer` | 0-3 | 正解のインデックス |
| `explanation` | string | 解説（短めに） |

## カスタマイズ

### 出題数を変更

`src/hooks/useQuiz.ts` の `QUESTIONS_PER_GAME` を変更：

```typescript
const QUESTIONS_PER_GAME = 10; // ← この数を変更
```

### 難易度フィルタを追加（将来の拡張）

`startGame` 関数内でフィルタリング可能：

```typescript
const filtered = (questionsData as Question[])
  .filter(q => q.difficulty <= selectedDifficulty);
```

## 注意事項

- 問題作りが楽しくて実装が止まりがち → **先に10問で完成させる**のがコツ
- 固有名詞（発売年・仕様）は検証が必要 → **原理・用語中心**が安全
- 難易度調整は `difficulty` フィールドで後からフィルタ可能

## 技術スタック

- Vite 5.x
- React 18.x
- TypeScript 5.x

## 📚 学習リソース

このプロジェクトは学習教材としても活用できます！

- **[📖 学習ガイド (LEARNING.md)](LEARNING.md)** - 非エンジニア向けのWeb開発入門
  - レッスン1: Web開発の全体像
  - レッスン3: CSS - 見た目を美しく
  - レッスン6: コンポーネントを理解する
  - その他、全10レッスン

- **[📋 クイックリファレンス (QUICK-REFERENCE.md)](QUICK-REFERENCE.md)** - よく使う知識を素早く参照

プログラミング初心者の方は、まず学習ガイドから始めることをおすすめします！
